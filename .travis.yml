dist: xenial

language: node_js

services:
    - docker

env:
    - DOCKER_COMPOSE_VERSION=1.4.2

node_js:
    - "lts/dubnium"

before_install:
    - cd smart-contracts

install:
    - yarn install --frozen-lockfile

script:
    - yarn test:ci
    - yarn coverage:ci
